/**
 * Minimal stub so @testing-library/jest-dom can import `aria-query`.
 * We only export the bits jest‑dom reads: roles map and element roles.
 */
export const roles = new Map();
export const elementRoles = new Map();

