specVersion: "v1"
serviceId: "project5"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "project5-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
